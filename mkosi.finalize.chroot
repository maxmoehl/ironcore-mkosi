#!/bin/sh

set -ex

# "uninitialized" ensures the image has proper first-boot semantics, see
# machine-id(5), section "First Boot Semantics" for a detailed explanation of
# the different states.
echo uninitialized > /etc/machine-id

# Add group for password-less sudo.
groupadd wheel
