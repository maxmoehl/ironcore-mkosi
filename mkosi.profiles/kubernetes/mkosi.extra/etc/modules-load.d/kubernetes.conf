# overlay: containerd snapshotter. nf_conntrack: kube-proxy (nftables mode)
# NAT tracking. br_netfilter is intentionally absent — FeCNI uses
# point-to-point veth pairs, no bridges, and no iptables exist in this image.
overlay
nf_conntrack
